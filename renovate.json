{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": [
    "config:recommended",
    "docker:pinDigests",
    "helpers:pinGitHubActionDigests",
    ":dependencyDashboard",
    ":semanticCommits"
  ],
  "timezone": "America/Los_Angeles",
  "schedule": ["* 0-6 * * 1"],
  "labels": ["dependencies", "supply-chain"],
  "commitMessagePrefix": "chore(deps):",
  "prConcurrentLimit": 6,
  "prHourlyLimit": 0,
  "dependencyDashboardTitle": "Renovate: weekly dependency dashboard",
  "customManagers": [
    {
      "customType": "regex",
      "description": "Pinned CLI/tool versions declared as ARG *_VERSION in the devlaptop and provisioner images, annotated with a `# renovate:` datasource hint on the preceding line.",
      "managerFilePatterns": ["devlaptop/Dockerfile", "provisioner/Dockerfile"],
      "matchStrings": [
        "# renovate: datasource=(?<datasource>\\S+) depName=(?<depName>\\S+)( versioning=(?<versioning>\\S+))?\\nARG \\S+=(?<currentValue>\\S+)"
      ]
    },
    {
      "customType": "regex",
      "description": "Kaniko builder image pinned in Helm values and the controller's generated values template.",
      "managerFilePatterns": [
        "charts/base-infrastructure/values.yaml",
        "charts/workspace/values.yaml",
        "charts/workspace/tests/test-values.yaml",
        "charts/workspace-controller/controller.py"
      ],
      "matchStrings": [
        "kanikoImage: (?<depName>gcr\\.io/kaniko-project/executor):(?<currentValue>[^\\s@\"]+)(@(?<currentDigest>sha256:[a-f0-9]+))?"
      ],
      "datasourceTemplate": "docker"
    },
    {
      "customType": "regex",
      "description": "oauth2-proxy — THE authentication gate for every workspace and the admin console. Pinned tag+digest in both chart templates; the Helm tests assert the same string, so they are listed here too and must bump together or CI fails. See issue #567.",
      "managerFilePatterns": [
        "charts/workspace/templates/oauth2-proxy.yaml",
        "charts/workspace-controller/templates/oauth2-proxy.yaml",
        "charts/workspace/tests/oauth2_proxy_test.yaml",
        "charts/workspace-controller/tests/oauth2-proxy_test.yaml"
      ],
      "matchStrings": [
        "(?<depName>quay\\.io/oauth2-proxy/oauth2-proxy):(?<currentValue>[^\\s@\"]+)@(?<currentDigest>sha256:[a-f0-9]+)"
      ],
      "datasourceTemplate": "docker"
    }
  ],
  "packageRules": [
    {
      "description": "Group the devlaptop + provisioner image CLI/tool bumps into one weekly PR.",
      "matchFileNames": ["devlaptop/Dockerfile", "provisioner/Dockerfile"],
      "groupName": "devlaptop CLI tools"
    },
    {
      "description": "Kaniko builder image — pin tag + digest, keep on its own.",
      "matchDepNames": ["gcr.io/kaniko-project/executor"],
      "groupName": "kaniko builder image"
    },
    {
      "description": "oauth2-proxy — the auth boundary. Never batch it with anything else, and label it security so it is reviewed as such, not as routine dependency noise.",
      "matchDepNames": ["quay.io/oauth2-proxy/oauth2-proxy"],
      "groupName": "oauth2-proxy (auth gate)",
      "addLabels": ["security"]
    },
    {
      "description": "Base OS/node images — pin + track digests together.",
      "matchManagers": ["dockerfile"],
      "matchDepNames": ["node", "ubuntu"],
      "groupName": "base container images"
    },
    {
      "description": "Pin and update GitHub Actions by digest, grouped.",
      "matchManagers": ["github-actions"],
      "groupName": "github-actions"
    },
    {
      "description": "SPA/controller dev dependencies are low-risk — batch them.",
      "matchManagers": ["npm"],
      "matchDepTypes": ["devDependencies"],
      "groupName": "npm dev dependencies"
    }
  ],
  "vulnerabilityAlerts": {
    "labels": ["security", "dependencies"],
    "schedule": ["at any time"]
  },
  "osvVulnerabilityAlerts": true
}

{
  "name": "kube-coder-dashboard",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage",
    "test:ui": "vitest --ui"
  },
  "dependencies": {
    "@preact/signals": "1.3.4",
    "d3-drag": "^3.0.0",
    "d3-force": "^3.0.0",
    "d3-scale": "^4.0.2",
    "d3-selection": "^3.0.0",
    "dompurify": "^3.4.3",
    "marked": "^18.0.3",
    "preact": "10.24.3"
  },
  "devDependencies": {
    "@preact/preset-vite": "2.9.1",
    "@testing-library/jest-dom": "6.6.3",
    "@testing-library/preact": "3.2.4",
    "@testing-library/user-event": "14.5.2",
    "@types/d3-drag": "^3.0.7",
    "@types/d3-force": "^3.0.10",
    "@types/d3-scale": "^4.0.9",
    "@types/d3-selection": "^3.0.11",
    "@types/node": "22.10.1",
    "@vitest/coverage-v8": "^2.1.4",
    "happy-dom": "20.8.9",
    "playwright": "^1.60.0",
    "playwright-core": "1.50.0",
    "typescript": "5.6.3",
    "vite": "6.4.3",
    "vitest": "2.1.9"
  },
  "_comment_esbuild": "Pin one esbuild: vite 6 needs 0.25.x but vitest 2.1.9 (bundled vite 5) pulls 0.21.5, and yarn hoisting mixes them -> 'Host version 0.21.5 does not match binary 0.25.12'. Remove once vitest is on v3.",
  "_comment_brace_expansion": "GHSA-mh99-v99m-4gvg (uncatchable OOM) affects every brace-expansion <=5.0.7 with no backported fix, so all instances must reach 5.0.8. That requires minimatch >=10 (older majors expect the pre-5.x default-function export). Only test-exclude/glob (vitest coverage) consume minimatch here; both verified working on 10.2.5. Remove both pins once dependents require brace-expansion ^5.0.8 on their own.",
  "resolutions": {
    "esbuild": "0.25.12",
    "minimatch": "^10.2.5",
    "brace-expansion": "^5.0.8"
  },
  "engines": {
    "node": ">=20"
  }
}

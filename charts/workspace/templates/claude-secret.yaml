{{- if .Values.claude.apiKey }}
apiVersion: v1
kind: Secret
metadata:
  name: claude-secrets-{{ .Values.user.name }}
  namespace: {{ .Values.namespace }}
type: Opaque
stringData:
  anthropic-api-key: {{ .Values.claude.apiKey | quote }}
{{- end }}

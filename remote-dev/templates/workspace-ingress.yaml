{{- range .Values.users }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ws-{{ .name }}
  namespace: {{ $.Values.namespace }}
  labels:
    app: ws-{{ .name }}
    {{- with $.Values.advanced.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- if $.Values.ingress.tls.enabled }}
    cert-manager.io/cluster-issuer: {{ $.Values.ingress.tls.clusterIssuer }}
    {{- end }}
    {{- if eq $.Values.ingress.auth.type "basic" }}
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: {{ $.Values.ingress.auth.secretName }}
    nginx.ingress.kubernetes.io/auth-realm: "Authentication Required"
    nginx.ingress.kubernetes.io/auth-response-headers: "Remote-User,Remote-Name,Remote-Email"
    nginx.ingress.kubernetes.io/proxy-buffer-size: "16k"
    nginx.ingress.kubernetes.io/proxy-buffers-number: "8"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "300"
    nginx.ingress.kubernetes.io/upstream-keepalive-timeout: "300"
    nginx.ingress.kubernetes.io/auth-keepalive: "300"
    nginx.ingress.kubernetes.io/auth-satisfy: "any"
    {{- end }}
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/websocket-services: "ws-{{ .name }}"
    {{- with $.Values.advanced.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ $.Values.ingress.className | quote }}
  {{- if $.Values.ingress.tls.enabled }}
  tls:
  - hosts: 
    - {{ .host | quote }}
    secretName: {{ $.Values.ingress.tls.secretName }}
  {{- end }}
  rules:
  - host: {{ .host }}
    http:
      paths:
      - path: /
        pathType: ImplementationSpecific
        backend:
          service:
            name: ws-{{ .name }}
            port: 
              number: 8080
      {{- if .forwardPorts }}
      {{- range .forwardPorts }}
      - path: {{ .path }}
        pathType: Prefix
        backend:
          service:
            name: ws-{{ .name }}
            port: 
              number: {{ .port }}
      {{- end }}
      {{- end }}
---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ws-{{ .name }}-terminal
  namespace: {{ $.Values.namespace }}
  labels:
    app: ws-{{ .name }}
    {{- with $.Values.advanced.labels }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
  annotations:
    {{- if $.Values.ingress.tls.enabled }}
    cert-manager.io/cluster-issuer: {{ $.Values.ingress.tls.clusterIssuer }}
    {{- end }}
    {{- if eq $.Values.ingress.auth.type "basic" }}
    nginx.ingress.kubernetes.io/auth-type: basic
    nginx.ingress.kubernetes.io/auth-secret: {{ $.Values.ingress.auth.secretName }}
    nginx.ingress.kubernetes.io/auth-realm: "Authentication Required"
    nginx.ingress.kubernetes.io/auth-response-headers: "Remote-User,Remote-Name,Remote-Email"
    nginx.ingress.kubernetes.io/proxy-buffer-size: "16k"
    nginx.ingress.kubernetes.io/proxy-buffers-number: "8"
    nginx.ingress.kubernetes.io/proxy-connect-timeout: "300"
    nginx.ingress.kubernetes.io/upstream-keepalive-timeout: "300"
    nginx.ingress.kubernetes.io/auth-keepalive: "300"
    nginx.ingress.kubernetes.io/auth-satisfy: "any"
    {{- end }}
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    nginx.ingress.kubernetes.io/websocket-services: "ws-{{ .name }}"
    nginx.ingress.kubernetes.io/rewrite-target: /$2
    nginx.ingress.kubernetes.io/use-regex: "true"
    {{- with $.Values.advanced.annotations }}
    {{- toYaml . | nindent 4 }}
    {{- end }}
spec:
  ingressClassName: {{ $.Values.ingress.className | quote }}
  {{- if $.Values.ingress.tls.enabled }}
  tls:
  - hosts: 
    - {{ .host | quote }}
    secretName: {{ $.Values.ingress.tls.secretName }}
  {{- end }}
  rules:
  - host: {{ .host }}
    http:
      paths:
      - path: /terminal(/|$)(.*)
        pathType: ImplementationSpecific
        backend:
          service:
            name: ws-{{ .name }}
            port: 
              number: 7681
---
{{- end }}